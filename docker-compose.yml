services:
  discovery-server:
    build: .
    container_name: discovery-server
    ports:
      - "8761:8761"
    environment:
      - JAVA_OPTS=-Xmx512m
    networks:
      - shared_network
networks:
  shared_network:
    external: true